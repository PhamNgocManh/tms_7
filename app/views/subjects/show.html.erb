<% provide(:title, @subject.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="subject_info">
      <h1>
        <%= @subject.name %>|<%= @subject.start_at %>
      </h1>
    </section>
  </aside>
</div>